---
layout: default
title: 编译复查报告
parent: 附录
nav_order: 4
---

# Chisel 代码编译复查报告（verify/）

> 目标：用真实工具链**复查页面所展示的 Chisel 代码**（学生骨架 + 环境 + 测试）能否编译通过。
> 结论：**19/19 实验的主代码与测试代码全部编译通过**（Mill 1.0.4 + JDK 17 + Chisel 3.5.6 + Scala 2.13.12）；
> 期间发现并修复 **7 类真实缺陷**（其中 5 类是"照原稿写下来不会编译"的硬错误）。
> 本报告只覆盖**编译**；**未跑仿真**（`chisel.test`），也未涉及 Verilog/Vivado 流程。

## 1. 复查环境

| 项 | 值 |
|---|---|
| Mill | 1.0.4（`mill-dist-1.0.4.exe`，native 启动器） |
| JDK | 17.0.16（Mill 按 `//| mill-jvm-version: 17` 自动下载 Temurin 17） |
| Scala | 2.13.12（Mill 自动拉取） |
| Chisel | 3.5.6 |
| ChiselTest / ScalaTest | 0.6.2 / 3.2.19（treadle 后端，纯 JVM） |
| 主机 | Windows 11，`C:\Users\HP\source\other\Verilog2Chisel` |

## 2. 复查方法

```bash
# 1) 把源码复制到独立工作区（不污染 taskvscode/，也不产生 out/ 目录）
#    verify/expN/chisel  ← taskvscode/expN/chisel
#    verify/chisel-common ← taskvscode/chisel-common      （exp7~exp23 共享库）
# 2) 逐实验编译主代码 + 测试代码
cd verify/expN/chisel
../../../mill-dist-1.0.4.exe --no-daemon chisel.compile       # 主代码
../../../mill-dist-1.0.4.exe --no-daemon chisel.test.compile  # 测试代码
```

- 脚本：`verify/run.sh`（一键同步 + 全量编译，日志落在 `verify/logs/expN-{main,test}.log`）。
- 每个实验都是独立 Mill 工程；`exp5/exp6/exp17/exp20` 自包含，其余 15 个引用 `verify/chisel-common`。

## 3. 发现并修复的缺陷（7 类）

| # | 缺陷 | 影响面 | 修法 |
|---|---|---|---|
| 1 | `//| mill-version:` 头**不在文件最开头**（前面有注释块）→ `Invalid YAML header comment at build.mill:17` | 全部 19 个 `build.mill` | 把 `//|` 两行提到文件第一行 |
| 2 | `def ivyDeps = Agg(...)`：Mill 1.x 无 `Agg`，且 `ivyDeps` 已改名 | 全部 19 个 | `def mvnDeps = Seq(mvn"...")` |
| 3 | `def sources = T.sources(...)`：Mill 1.x 无 `T.sources`（沙箱要求显式声明） | 全部 19 个 | `def sources = Task.Sources(...)` |
| 4 | `millSourcePath`：Mill 1.x 改名，且默认推断成 `<模块名>/src`（实际是 `chisel/src`） | 全部 19 个 | `def moduleDir = super.moduleDir / os.up` |
| 5 | `MuxLookup(k, d)(Seq(...))`：Chisel 3.5 的 `MuxLookup` 是**单参数列表**（3 个参数） | `Confreg.scala`（共享库 + exp6 各 3 处） | 改为 `MuxLookup(k, d, Seq(...))` |
| 6 | `val reset = RegNext(...)`：与 `Module.reset`（final 成员）冲突 | exp5 / exp6 学生模块 | 重命名 `reset_r` |
| 7 | 续行以 `\|` 开头（Verilog 写法，非法 Scala） | exp6 `Alu.scala`（含 solution）9 行 | `\|` 移到上一行末尾 |
| 8 | 测试文件直接写 `0.U` / `false.B` 却未 `import chisel3._` | `TraceHarness.scala`、`TlbSpec.scala`、`CacheSpec.scala` | 补 `import chisel3._` |

> 第 5~8 项是**代码缺陷**（不改就编译不过）；第 1~4 项是**构建脚本与 Mill 1.x 的接口不匹配**。
> 修复只改 `taskvscode/`（真源），`verify/` 每次从真源重新同步。

## 4. 复查结果

（下表由 `verify/run.sh` 的最终一次干净运行生成；`main` = `chisel.compile`，`test` = `chisel.test.compile`，0 = 通过）

<!-- RESULTS:BEGIN -->
| 实验 | chisel.compile（主代码） | chisel.test.compile（测试） |
|---|---|---|
| exp5 | ✅ 通过 | ✅ 通过 |
| exp6 | ✅ 通过 | ✅ 通过 |
| exp7 | ✅ 通过 | ✅ 通过 |
| exp8 | ✅ 通过 | ✅ 通过 |
| exp9 | ✅ 通过 | ✅ 通过 |
| exp10 | ✅ 通过 | ✅ 通过 |
| exp11 | ✅ 通过 | ✅ 通过 |
| exp12 | ✅ 通过 | ✅ 通过 |
| exp13 | ✅ 通过 | ✅ 通过 |
| exp14 | ✅ 通过 | ✅ 通过 |
| exp15 | ✅ 通过 | ✅ 通过 |
| exp16 | ✅ 通过 | ✅ 通过 |
| exp17 | ✅ 通过 | ✅ 通过 |
| exp18 | ✅ 通过 | ✅ 通过 |
| exp19 | ✅ 通过 | ✅ 通过 |
| exp20 | ✅ 通过 | ✅ 通过 |
| exp21 | ✅ 通过 | ✅ 通过 |
| exp22 | ✅ 通过 | ✅ 通过 |
| exp23 | ✅ 通过 | ✅ 通过 |

**结论：19/19 全部通过**（日志见 `verify/logs/expN-{main,test}.log`）。
<!-- RESULTS:END -->

## 5. 未覆盖的部分

| 项 | 状态 | 说明 |
|---|---|---|
| 仿真（`chisel.test`） | ❌ 未跑 | exp6~exp23 需先解包运行件：`node ../cod-WHU.github.io/tools/unpack-assets.mjs`（`.mif` + `golden_trace.txt`） |
| elaboration（生成 Verilog） | ⚠️ 未单独跑 | 编译通过不等于 elaboration 通过（未驱动/组合环等要到 elaboration 才报） |
| 原 Verilog 仿真 / Vivado | ❌ 未做 | 需要 Vivado 与 Xilinx IP |
| 上板 | ❌ 未做 | 需要 FPGA |

> 下一步建议（按性价比排序）：
> 1. 跑 `chisel.test`（exp5/exp17/exp20 不需要运行件，可直接跑；其余先解包）；
> 2. 给每个实验加一个 `Elaborate` main，跑 `runMain` 让 FIRRTL 检查未驱动/位宽/组合环；
> 3. 有 Vivado 环境时再验原 Verilog 流程。

## 6. 复现方式

```bash
cd C:/Users/HP/source/other/Verilog2Chisel
bash verify/run.sh                 # 全量：同步 + 编译 19 个实验（约 20 分钟）
cat verify/SUMMARY.txt             # 结果一览
grep -h "\[error\]" verify/logs/*.log   # 若有失败，看具体报错
```
