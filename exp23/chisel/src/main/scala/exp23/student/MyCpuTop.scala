// SPDX-License-Identifier: BSD-3-Clause
// ============================================================================
// 学生模块（从零实现 + CACOP）：myCPU —— 五级流水线，AXI 接口，I/D Cache，CACOP
//   对应原实验：code/myCPU/（原实验环境**不提供** myCPU 目录，由学生自己实现）
//
// 本实验（实践任务23）的教学意图 = 从零实现（在 exp22 基础上增量）：
//   在已集成 I/D Cache 的 CPU 中增加 **CACOP 指令**（Cache 操作）实现。
//   Chisel 版只给接口骨架 + 20 处 TODO(实现)，内部逻辑全部留空（???）。
//
// CACOP 语义（详细见原书 10.2.4 / ../../chisel4agent/09-CPU开发场景速查.md）：
//   * 指令格式：`cacop code, rj, si12`，其中 `code[4:3]` 选择 Cache：
//     0 = 按地址操作 ICache，1 = 按地址操作 DCache，
//     2 = 按索引操作 ICache，3 = 按索引操作 DCache；
//     `code[2:0]` 为具体操作（如 Store Tag / 无效化 / 写回并无效化等）；
//   * 按地址操作时，虚地址为 `rj + si12`，需要先做地址转换（DMW/TLB）；
//   * 按索引操作时，用虚地址中的 index 字段直接定位 Cache 行；
//   * 操作需要与流水线配合：可能要停顿，并保证后续取指/访存看到一致的数据。
// ============================================================================

package exp23.student

import chisel3._
import chisel3.util._
import envlib.soc.LACpuAxi

class MyCpuTop extends LACpuAxi {
  // io 由基类 LACpuAxi 提供（类型 envlib.soc.CpuIOAxi）：ar/r/aw/w/b 五通道 + debug_wb_*

  // --------------------------------------------------------------------------
  // ① IF 级：取指（AR/R 通道）
  // --------------------------------------------------------------------------
  // TODO(实现 1/20)：pc / nextpc / 取指的 AR 与 R 通道
  io.arid    := ???
  io.araddr  := ???
  io.arlen   := ???
  io.arsize  := ???
  io.arburst := ???
  io.arlock  := ???
  io.arcache := ???
  io.arprot  := ???
  io.arvalid := ???
  io.rready  := ???

  // --------------------------------------------------------------------------
  // ② IF/ID 流水寄存器
  // --------------------------------------------------------------------------
  // TODO(实现 2/20)：IF/ID 流水寄存器

  // --------------------------------------------------------------------------
  // ③ ID 级：译码 + 读寄存器堆 + 分支判断
  //    本实验需**新增译码**：CACOP。
  // --------------------------------------------------------------------------
  // TODO(实现 3/20)：指令译码（含 CACOP）、立即数、寄存器堆读、分支判断

  // --------------------------------------------------------------------------
  // ④ ID/EX 流水寄存器
  // --------------------------------------------------------------------------
  // TODO(实现 4/20)：ID/EX 流水寄存器

  // --------------------------------------------------------------------------
  // ⑤ EX 级：ALU
  // --------------------------------------------------------------------------
  // TODO(实现 5/20)：ALU 运算

  // --------------------------------------------------------------------------
  // ⑥ EX/MEM 流水寄存器
  // --------------------------------------------------------------------------
  // TODO(实现 6/20)：EX/MEM 流水寄存器

  // --------------------------------------------------------------------------
  // ⑦ MEM 级：访存（AW/W/B 通道）
  // --------------------------------------------------------------------------
  // TODO(实现 7/20)：数据的 AW/W/B 通道 + ALE 检测
  io.awid    := ???
  io.awaddr  := ???
  io.awlen   := ???
  io.awsize  := ???
  io.awburst := ???
  io.awlock  := ???
  io.awcache := ???
  io.awprot  := ???
  io.awvalid := ???
  io.wid     := ???
  io.wdata   := ???
  io.wstrb   := ???
  io.wlast   := ???
  io.wvalid  := ???
  io.bready  := ???

  // --------------------------------------------------------------------------
  // ⑧ MEM/WB 流水寄存器
  // --------------------------------------------------------------------------
  // TODO(实现 8/20)：MEM/WB 流水寄存器

  // --------------------------------------------------------------------------
  // ⑨ WB 级：写回 + 调试信息
  // --------------------------------------------------------------------------
  // TODO(实现 9/20)：写回寄存器堆 + debug_wb_* 输出
  io.debug_wb_pc       := ???
  io.debug_wb_rf_we    := ???
  io.debug_wb_rf_wnum  := ???
  io.debug_wb_rf_wdata := ???

  // --------------------------------------------------------------------------
  // ⑩ 数据相关处理：前递 + load-use 阻塞
  // --------------------------------------------------------------------------
  // TODO(实现 10/20)：前递通路 + load-use 阻塞

  // --------------------------------------------------------------------------
  // ⑪ CSR 寄存器堆与 CSR 指令（含 TLB 相关 CSR 与 DMW）
  // --------------------------------------------------------------------------
  // TODO(实现 11/20)：CSR 寄存器堆 + csrrd/csrwr/csrxchg

  // --------------------------------------------------------------------------
  // ⑫ 例外与 ertn
  // --------------------------------------------------------------------------
  // TODO(实现 12/20)：例外入口 + ertn 返回

  // --------------------------------------------------------------------------
  // ⑬ 中断与定时器/计数器
  // --------------------------------------------------------------------------
  // TODO(实现 13/20)：中断使能与仲裁 + 定时器 + rdcnt* 指令

  // --------------------------------------------------------------------------
  // ⑭ AXI 事务状态机（含 ID 管理）
  // --------------------------------------------------------------------------
  // TODO(实现 14/20)：AXI 读写事务状态机与 ID 管理

  // --------------------------------------------------------------------------
  // ⑮ AXI 请求未完成时的流水线互锁
  // --------------------------------------------------------------------------
  // TODO(实现 15/20)：AXI 请求未完成时的流水线停顿/互锁

  // --------------------------------------------------------------------------
  // ⑯ TLB 集成与 TLB 指令（exp18）
  // --------------------------------------------------------------------------
  // TODO(实现 16/20)：TLB 例化 + TLBSRCH/TLBRD/TLBWR/TLBFILL/INVTLB

  // --------------------------------------------------------------------------
  // ⑰ 虚实地址映射与 TLB 例外（exp19）
  // --------------------------------------------------------------------------
  // TODO(实现 17/20)：DMW + TLB 地址转换 + TLB 例外（TLBR/PIL/PIS/PME/PPI）

  // --------------------------------------------------------------------------
  // ⑱ ICache 集成与 Burst 取指（exp21）
  // --------------------------------------------------------------------------
  // TODO(实现 18/20)：ICache 例化 + Burst 读填充 + 取指停顿

  // --------------------------------------------------------------------------
  // ⑲ DCache 集成（exp22）
  // --------------------------------------------------------------------------
  // TODO(实现 19/20)：DCache 例化 + 写通路 + 脏行写回 + 访存停顿

  // --------------------------------------------------------------------------
  // ⑳ CACOP 指令（本实验核心）
  //    按 code[4:3] 选择 ICache/DCache 与"按地址/按索引"，
  //    按 code[2:0] 执行具体 Cache 操作；按地址操作需先做地址转换。
  // --------------------------------------------------------------------------
  // TODO(实现 20/20)：CACOP 译码 + Cache 操作通路 + 流水线配合
}
