---
layout: default
title: 工作流指南
---

# Chisel → Verilog → 仿真 → 上板 工作流配置指南

> 本页把「写完 Chisel → 生成 Verilog → 跑仿真 → 上板验证」这条链路讲清楚，
> 命令与版本都在本机实测过（下方标 ✅ 的段落有实测输出；Vivado 段落标注为 ❓未实测）。

## 0. 总览

```text
Chisel 源码 (expN/chisel/src/main/scala/...)
   │  ① mill chisel.compile               语法 / 类型 / Chisel API
   ▼
elaboration（ChiselStage）                 Chisel 语义：未驱动、位宽、组合环
   │  ② mill chisel.runMain Elaborate
   ├─→ generated/*.fir  (FIRRTL 中间表示)
   └─→ generated/*.v    (Verilog)
         │
         ├─→ ③ 仿真  mill chisel.test（treadle，纯 JVM）
         │         可选：Verilator 后端 / 生成的 .v 交给 Vivado XSim
         │
         └─→ ④ 上板  Vivado：综合 → 实现 → 生成比特流 → 下载
                    器件 xc7a200tfbg676-1（原工程 create_project.tcl 指定）
```

## 1. 环境与版本矩阵（✅ 实测）

| 组件 | 版本 | 说明 |
|---|---|---|
| JDK | **17**（本机用 17.0.16 / 21 亦可） | `build.mill` 里 `//| mill-jvm-version: 17`，Mill 会自动下载 Temurin 17 |
| Mill | **1.0.4** | native 启动器，[下载地址](https://repo1.maven.org/maven2/com/lihaoyi/mill-dist/1.0.4/)：`mill-dist-1.0.4.exe`（34.9 MB） |
| Scala | **2.13.10** | 必须与 chisel3-plugin 的交叉版本一致（见 §5 踩坑） |
| Chisel | **3.6.0** | `mvn"edu.berkeley.cs::chisel3:3.6.0"` |
| Chisel 编译器插件 | **chisel3-plugin 3.6.0** | `scalacPluginMvnDeps`，**必装**（缺它编译能过、elaboration 必挂） |
| ChiselTest | **0.6.2** + ScalaTest 3.2.19 | treadle 后端（纯 JVM，不需要 Verilator） |
| Verilator（可选） | 5.x | 只想更快仿真时用；不装也能跑 |
| Vivado（上板） | 2019.x 及以上 | 本机未装，§4 按仓库脚本整理 |

首次编译会从 Maven Central 拉约 200–300 MB 依赖，缓存在 `~/.cache/coursier`（Windows：`%USERPROFILE%\AppData\Local\Coursier\cache`）。

## 2. ① 编译 + ② 生成 Verilog

### 2.1 编译

```bash
cd expN/chisel
../../../mill-dist-1.0.4.exe --no-daemon chisel.compile
```

`--no-daemon` 可避免 Mill 守护进程占用目录锁（连续切换实验目录时尤其有用）。

### 2.2 导出 Verilog

Chisel 3.6 的 API 是 `ChiselStage`。注意 **`ChiselStage.emitVerilog(gen)` 只返回字符串、不落盘**，要落盘得用 `execute` + `ChiselGeneratorAnnotation`：

```scala
// expN/chisel/src/main/scala/Elaborate.scala
import chisel3.stage.{ChiselStage, ChiselGeneratorAnnotation}

object Elaborate extends App {
  (new ChiselStage).execute(
    Array("--target-dir", "generated"),
    Seq(ChiselGeneratorAnnotation(() => new expN.student.MyCpuTop))
  )
}
```

```bash
./mill chisel.runMain Elaborate
ls generated/          # MyCpuTop.fir  MyCpuTop.v  MyCpuTop.anno.json
```

> ✅ 实测（exp5 参考解）：生成 `MinicpuTop.v` 共 **311 行**，端口形如
> `input clock, input reset, input io_resetn, output io_inst_sram_we, …`。

**接入 SoC 前要注意两件事**：
1. Chisel 生成的时钟/复位名是 `clock` / `reset`（高有效），本仓库的 SoC 环境用 `resetn`（低有效）——要么在 Chisel 里显式声明 `io.resetn`（本仓库的做法），要么在 Verilog 侧做适配；
2. 端口带 `io_` 前缀，替换原 `mycpu_top.v` 时需要名字对齐。

## 3. ③ 仿真

### 3.1 ChiselTest（默认，treadle 后端）

```bash
cd expN/chisel
./mill chisel.test          # 全部测试
./mill chisel.test.testOnly exp7.test.MyCpuTbSpec   # 只跑某个 spec
```

> ✅ 实测（exp5 参考解）：`Tests: succeeded 9, failed 0`，全部断言通过。

**运行件**：exp6~exp23 的测试会读 `code/func/obj/*.mif` 与 `code/gettrace/golden_trace.txt`。
站点仓库里已打包为 `assets/sim-assets.tar.gz`：

```bash
node tools/unpack-assets.mjs        # 解包到 expN/code/func/obj 与 expN/code/gettrace
node tools/unpack-assets.mjs --check  # 只校验 sha256
```

exp5 / exp17 / exp20 不需要运行件，可直接跑。

### 3.2 看波形

```scala
import chiseltest._

test(new MyCpuTop).withAnnotations(Seq(WriteVcdAnnotation)) { dut => … }
```

波形落在 `test_run_dir/<测试名>/…vcd`，用 GTKWave / Surfer 打开。

### 3.3 换 Verilator 后端（可选）

```scala
test(new MyCpuTop).withAnnotations(Seq(VerilatorBackendAnnotation)) { dut => … }
```
需要先装 [Verilator](https://verilator.org/)（Windows 上建议 WSL2 或 scoop）。

### 3.4 用生成的 Verilog 做仿真

`generated/*.v` 可直接交给 Vivado XSim 或 Icarus Verilog，跑原实验的 `testbench/`（例如 `code/soc_verify/soc_bram/testbench`）。注意原 tb 例化的顶层是 `soc_lite_top`，替换学生模块时需要保持端口一致。

## 4. ④ 上板（Vivado）

> ❓ 本节按仓库内原工程脚本整理，本机未装 Vivado，**未实测**。

原实验工程已经带好脚本（`code/soc_verify/<变体>/run_vivado/create_project.tcl`）：

| 项 | 取值 |
|---|---|
| 器件 | `xc7a200tfbg676-1`（Artix-7 XC7A200T） |
| 时钟约束 | `create_clock -period 10.000`（100 MHz），引脚 `AC19` |
| 复位 | `resetn` 引脚 `Y3` |
| LED | `K23 / J21 / H23 …` |
| IP | `rtl/xilinx_ip/*/*.xci`（`clk_pll`、`inst_ram`、`data_ram`）——**必须存在**，否则 tcl 里 `add_files` 会落空 |

流程：

```bash
cd expN/code/soc_verify/soc_bram/run_vivado
vivado -mode batch -source create_project.tcl     # 生成 ./project/loongson.xpr
# 之后在 Vivado GUI 里：Generate Bitstream → Hardware Manager → Program Device
```

把 Chisel 版接到板上，有两条路：

1. **替换 RTL**：把 `generated/*.v` 加入工程，移除原 `myCPU/*.v`，端口对齐后重新综合（推荐先跑 XSim 确认功能）；
2. **只在仿真里用**：上板仍用原 Verilog，Chisel 版作为等价实现对照。

func 测试程序的工具链（原实验）：LoongArch32 交叉编译器
`loongarch32r-linux-gnusf-{gcc,as,ld,objcopy,objdump}`：

```bash
cd expN/code/func
make EXP=7          # 生成 obj/inst_ram.coe 等；EXP 取 0,6-16,18-19,21-23
```

## 5. 常见问题（都是本仓库踩过的坑）

| 现象 | 原因 | 处理 |
|---|---|---|
| `Invalid YAML header comment at build.mill:N` | `//| mill-version:` 不在文件最开头 | 把两行 `//|` 提到第一行 |
| `Not found: Agg` / `Cannot resolve chisel.ivyDeps` | Mill 1.x API 改名 | `mvnDeps` + `mvn"..."`；`Task.Sources`；`moduleDir` |
| `The Chisel compiler plugin is now required…` | 缺 `scalacPluginMvnDeps` | 加 `mvn"edu.berkeley.cs:::chisel3-plugin:3.6.0"` |
| 测试运行时 `NoClassDefFoundError: chisel3/internal/sourceinfo/SourceInfo` | chisel3 版本不一致（ChiselTest 0.6.x 依赖 chisel3 **3.6.0**，主代码却是 3.5.6） | 主代码与测试统一到 3.6.0 |
| `not found: chisel3-plugin_2.13.12:3.6.0` | 插件只发布 `_2.13.10` 交叉版本 | `scalaVersion = "2.13.10"` |
| `MifLoader / TraceLoader 找不到文件` | 缺运行件 | `node tools/unpack-assets.mjs` |
| `NotImplementedError` | 学生模块里的 `TODO` 还是 `???` | 预期行为：先实现再跑 |
| Mill 报目录被占用 / 锁文件删不掉 | 守护进程在跑 | `./mill shutdown` 或用 `--no-daemon` |

## 6. 参考

- [Chisel 官方文档](https://www.chisel-lang.org/docs) ｜ [Chisel API（3.6.0）](https://www.chisel-lang.org/api/3.6.0/)
- [ChiselTest 文档](https://github.com/ucb-bar/chiseltest)（0.6.2，treadle/Verilator 后端）
- [Mill 文档](https://mill-build.org/) ｜ [Mill 1.0.4 下载](https://repo1.maven.org/maven2/com/lihaoyi/mill-dist/1.0.4/)
- [Verilator](https://verilator.org/) ｜ [GTKWave](https://gtkwave.sourceforge.net/)
- [Verilog to Chisel（黄治豪）](https://zihaojf.github.io/Chisel-/chisel/%E7%AE%80%E4%BB%8B/)：Chisel 入门与流水线写法
- 教材：[《CPU 设计实战：LoongArch 版》](https://bookdown.org/loongson/_book3/)（汪文祥、邢金璋 等著）
